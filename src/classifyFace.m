function [out] = classifyFace(in)
    %{
        in : (M X N) matrix. M is number of people. N is number of group photo.
        out : (M X N) matrix. Classified version of in.
    %}

    [M N] = size(in);
    for i=1:M
    end
end
